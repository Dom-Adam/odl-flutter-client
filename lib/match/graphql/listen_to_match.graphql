subscription ListenToMatch($matchId: String!) {
  listenToMatch(matchId: $matchId) {
    legs {
      id
      points
      isFinished
      visits {
        id
        playerId
        isFinished
      }
    }
  }
}
